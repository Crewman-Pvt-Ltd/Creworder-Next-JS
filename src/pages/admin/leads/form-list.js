import React from 'react'
import Layout from '@/components/Layout'
import FormList from '@/components/leads/FormList';
const formlist = () => {

    return (
      <Layout type="admin">
        <FormList />
      </Layout>
    );
  };
  
  export default formlist;
